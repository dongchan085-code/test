name: Daily HF Papers Newsletter

on:
  schedule:
    # 매일 아침 9시 (한국시간 기준 KST 09:00 -> UTC 00:00) 
    - cron: '0 0 * * *'
  workflow_dispatch: # 버튼으로 수동 실행도 가능하게 설정

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Generate Newsletter (Run main.py)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HF_TOPIC: ${{ vars.HF_TOPIC || 'LLM' }} # GitHub Variables에서 주제 설정 가능
        run: |
          python main.py
          
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload GitHub Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # index.html이 있는 현재 디렉토리를 업로드합니다.
          path: '.'
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
